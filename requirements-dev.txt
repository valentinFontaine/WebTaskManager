# Dépendances de développement et de test.
# Installation : pip install -r requirements.txt -r requirements-dev.txt
#
# httpx est requis par fastapi.testclient (TestClient), qui ne l'embarque pas.
# Sans ces deux paquets, `pytest test_fastapi.py` ne démarre pas.

pytest>=8.0.0
httpx>=0.27.0
